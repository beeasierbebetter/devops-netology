# Домашнее задание к занятию «Системы контроля версий» `Котуков Евгений`

В .gitignore внутри директории terraform будут скрыты:
скрытая директория .terraform и все вложенные объекты, \
также все файлы с расширением .tfstate и содержащие .tfstate. в названии с любым другим расширением, \
скрыт файл crash.log и все файлы, начинающиеся на crash. с расширением log, \
аналогично скрыты все файлы с расширением .tfvars и .tfvars.json,
скрыты конкретные файлы override.tf и override.tf.json, а также все файлы, заканчивающиеся на \
_override.tf и _override.tf.json \
Еще из отслеживания в git исключается файл .terraform.tfstate.lock.info \
и файлы .terraformrc и terraform.rc
